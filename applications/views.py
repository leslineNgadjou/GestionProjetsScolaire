"""Vues candidatures — squelette pour l’étape suivante."""

from django.http import HttpResponse


def index(request):
    """Point d’entrée URL temporaire."""
    return HttpResponse('Module candidatures : les vues seront ajoutées ensuite.')
