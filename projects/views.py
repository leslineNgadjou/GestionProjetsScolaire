"""Vues projets — squelette pour l’étape CRUD."""

from django.http import HttpResponse


def index(request):
    """Point d’entrée URL temporaire."""
    return HttpResponse('Module projets : les vues CRUD seront ajoutées ensuite.')
